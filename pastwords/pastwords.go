//go:build ignore
// +build ignore

package pastwords

// REMOVED: pastwords package
//
// The original implementation of the pastwords package has been removed from
// the repository. This file is intentionally excluded from the normal Go build
// using the 'ignore' build tag so the package no longer participates in
// compilation. The original source remains available in the git history.
//
// NOTE: If you truly want the files deleted from the repository, remove them
// with `git rm` in your working tree and commit the deletion. This placeholder
// file keeps a clear record in the repository that the package was intentionally
// removed and avoids accidental build-time references.
